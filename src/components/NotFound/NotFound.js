// Title: Not Found
// Details: Component for showing not found text.
// Author: raadu

const NotFound = ({message}) => {
    return (
        <div>
            {message}
        </div>
    );
}
 
export default NotFound;